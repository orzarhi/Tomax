import { Input } from "./ui/input"

export const InputSearch = () => {
    return (
        <Input
            className="w-full max-w-sm mx-auto"
            placeholder="Search"
        />
    )
}
